package com.technext.patterns;

public class SlidingWindow {
  /** Sliding Window Technique function that operates on determining
   * the maximum sum given an array and the size of a sliding window. */
  public int slidingWindowMaxSum(int[] input, int k) {
    int maxSum = Integer.MIN_VALUE;

    int start = 0;
    int currSum = 0;

    for (int i = 0; i < input.length; i++) {

    }



  }
}
